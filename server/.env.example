PORT=8080
APP_ENV=development
DATABASE_URL=postgres://postgres:postgres@localhost:5434/warrantykeeper?sslmode=disable
JWT_SECRET=dev-secret-change-me

# Optional: set to use the real Anthropic-based OCR provider instead of the
# stub. Leave unset to keep using the stub (always low-confidence, requires
# manual entry).
# ANTHROPIC_API_KEY=
# ANTHROPIC_OCR_MODEL=claude-haiku-4-5-20251001
